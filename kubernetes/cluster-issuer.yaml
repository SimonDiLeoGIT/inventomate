apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-production
  annotations:
    cert-manager.io/disable-validation: "true"   # Anotación para desactivar la validación del webhook
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: fedesimone31@gmail.com
    privateKeySecretRef:
      name: letsencrypt-production
    solvers:
      - dns01:
          cloudflare:
            email: fedesimone31@gmail.com
            apiTokenSecretRef:
              name: cloudflare-token-secret
              key: NcABmzmORZSzjkG8gOq4qM9ONIrZHCmWpWWbSSJF
        selector:
          dnsZones:
            - "inventomate.xyz"
---

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nginx-dev-cloudflare
  namespace: cert-manager
spec:
  secretName: nginx-default-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "inventomate.xyz"
  dnsNames:
  - "inventomate.xyz"