apiVersion: apps/v1
kind: Deployment
metadata:
  name: cert-manager
  namespace: inventomate
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cert-manager
  template:
    metadata:
      labels:
        app: cert-manager
    spec:
      serviceAccountName: cert-manager
      containers:
      - name: cert-manager
        image: quay.io/jetstack/cert-manager-controller:v1.14.5
        args:
        - --dns01-recursive-nameservers=1.1.1.1:53,9.9.9.9:53
        - --dns01-recursive-nameservers-only
        - --pod-dns-policy=None
        - --dns01-recursive-nameservers=1.1.1.1:53,9.9.9.9:53
        - --dns01-recursive-nameservers-only
        - --pod-dns-policy=None
        - --leader-election-namespace=inventomate
        - --controllers='*'
        - --enable-certificate-owner-ref
        volumeMounts:
        - name: temp-ssl-storage
          mountPath: /tmp/ssl
      volumes:
      - name: temp-ssl-storage
        emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: cert-manager-webhook
  namespace: cert-manager
spec:
  selector:
    app: cert-manager
  ports:
    - protocol: TCP
      port: 443
      targetPort: 8443